media:
  input: assets/uploads
  output: /assets/uploads

content:
  - name: posts
    label: Projects
    path: _posts
    type: collection
    fields:
      - name: id
        label: "📃 ID"
        type: string
        required: true
        description: "NOTE: Remember to add a leading 0 if the number is single digit. Ex.: 05"
      - name: layout
        type: select
        options:
          values: [ blog ]
        default: blog
        required: true
        description: "NOTE: Don't touch this, it's here merely for structural purposes."
      - name: title
        label: 📇 Name
        type: string
        required: true
        description: "NOTE: Keep the name length ideally under 10 characters, otherwise it may (or may not) look wonky."
      - name: description
        label: 📇 Description
        type: text
        options:
          maxlength: 180
        required: true
        description: "NOTE: The total length MUST be between 200-280 characters, otherwise it WILL look wonky."
      - name: project-link
        label: 🔗 URL
        type: string
        required: true
      - name: custom_date
        label: 🗓️ Custom Date
        type: date
        description: "NOTE: Only enter a custom date if you need it, otherwise leave blank and it will default to today's date."
      - name: mainRole
        label: Main Role
        type: select
        options:
          values: ["Level Designer", "Environment Artist", "Gameplay Artist", "Sound Designer", "Promotional Artist", "Project Manager", "Other"]
        required: true
        default: "Level Designer"
        description: "NOTE: If you had multiple roles, select the most relevant. If you need to add more, contact Pablo."
      - name: game
        label: 🎮 Videogame 
        type: select
        options:
          values: ["Counter-Strike 2", "Counter-Strike: Global Offensive", "Other"]
        description: "NOTE: If you need to add more, contact Pablo."
        required: true
      - name: thumbnails
        label: 📷 Project Thumbnails
        type: object
        required: true
        fields:
          - name: thumbnails
            label: 📸Project Thumbnails
            fields:
              - name: thumbnail
                label: 📸 Thumbnail
                type: image
                options:
                  multiple: true
                  max: 9
        description: "NOTE: These are the slideshow images. The first 5 will be shown on the Main Page slideshow. Upload a minimum of 5 and a maximum of 12 for consistency."
      - name: sections
        label: Page Sections
        type: block
        list: true
        required: true
        blocks:
          - name: one-column
            label: Add Single-Column Section
            fields: 
              - name: content_blocks
                label: Content
                type: block
                list: true
                blocks: 
                  - name: text_block
                    label: 💬Text Block
                    fields:
                      - name: text
                        label: "Write your text here. NOTE: you can use custom styles. ALWAYS USE H3 FOR HEADINGS"
                        type: rich-text
                  - name: image_block
                    label: 📸 Image Block
                    fields:
                      - name: image
                        label: 📤 Upload your image here
                        type: image
                      - name: image_caption
                        label: 📜(Optional) Add a brief caption to show under the image
                        type: string
                  - name: video_block
                    label: 🎥 Video Block
                    fields:
                      - name: video_url
                        label: 🔗 Paste the video Embed URL
                        type: string
                      - name: video_caption
                        label: 📜(Optional) Add a brief caption to show under the video
                        type: string
                  - name: comparison_block
                    label: 🆚 Comparison Block
                    fields:
                      - name: comparison_first_image
                        label: 📤 Upload the first image
                        type: image
                      - name: comparison_first_note
                        label: 📜(Optional) Write a note to show at the bottom left
                        type: string
                      - name: comparison_second_image
                        label: 📤 Upload the second image
                        type: image
                      - name: comparison_second_note
                        label: 📜(Optional) Write a note to show at the bottom right
                        type: string
                      - name: comparison_caption
                        label: 📜 (Optional) Add a brief caption to show under the comparison component
                        type: string
                  - name: slideshow_block
                    label: 🎰 Slideshow
                    fields:
                    - name: slideshow_images
                      label: 📤📸 Select the uploaded images (Max. 9).
                      type: image
                      options:
                        multiple: true
                        max: 9
          - name: two-column
            label: Add Double-Column Section
            fields:
            - name: left-column
              label: Left Column Content
              type: block
              list: true
              blocks:
                  - name: text_block
                    label: 💬Text Block
                    fields:
                      - name: text
                        label: "Write your text here. NOTE: you can use custom styles. ALWAYS USE H3 FOR HEADINGS"
                        type: rich-text
                  - name: image_block
                    label: 📸 Image Block
                    fields:
                      - name: image
                        label: 📤 Upload your image here
                        type: image
                      - name: image_caption
                        label: 📜(Optional) Add a brief caption to show under the image
                        type: string
                  - name: video_block
                    label: 🎥 Video Block
                    fields:
                      - name: video_url
                        label: 🔗 Paste the video Embed URL
                        type: string
                      - name: video_caption
                        label: 📜(Optional) Add a brief caption to show under the video
                        type: string
                  - name: comparison_block
                    label: 🆚 Comparison Block
                    fields:
                      - name: comparison_first_image
                        label: 📤 Upload the first image
                        type: image
                      - name: comparison_first_note
                        label: 📜(Optional) Write a note to show at the bottom left
                        type: string
                      - name: comparison_second_image
                        label: 📤 Upload the second image
                        type: image
                      - name: comparison_second_note
                        label: 📜(Optional) Write a note to show at the bottom right
                        type: string
                      - name: comparison_caption
                        label: 📜 (Optional) Add a brief caption to show under the comparison component
                        type: string
                  - name: slideshow_block
                    label: 🎰 Slideshow
                    fields:
                    - name: slideshow_images
                      label: 📤📸 Select the uploaded images (Max. 9).
                      type: image
                      options:
                        multiple: true
                        max: 9
            - name: right-column
              label: Right Column Content
              type: block
              list: true
              blocks:
                  - name: text_block
                    label: 💬Text Block
                    fields:
                      - name: text
                        label: "Write your text here. NOTE: you can use custom styles. ALWAYS USE H3 FOR HEADINGS"
                        type: rich-text
                  - name: image_block
                    label: 📸 Image Block
                    fields:
                      - name: image
                        label: 📤 Upload your image here
                        type: image
                      - name: image_caption
                        label: 📜(Optional) Add a brief caption to show under the image
                        type: string
                  - name: video_block
                    label: 🎥 Video Block
                    fields:
                      - name: video_url
                        label: 🔗 Paste the video Embed URL
                        type: string
                      - name: video_caption
                        label: 📜(Optional) Add a brief caption to show under the video
                        type: string
                  - name: comparison_block
                    label: 🆚 Comparison Block
                    fields:
                      - name: comparison_first_image
                        label: 📤 Upload the first image
                        type: image
                      - name: comparison_first_note
                        label: 📜(Optional) Write a note to show at the bottom left
                        type: string
                      - name: comparison_second_image
                        label: 📤 Upload the second image
                        type: image
                      - name: comparison_second_note
                        label: 📜(Optional) Write a note to show at the bottom right
                        type: string
                      - name: comparison_caption
                        label: 📜 (Optional) Add a brief caption to show under the comparison component
                        type: string
                  - name: slideshow_block
                    label: 🎰 Slideshow
                    fields:
                    - name: slideshow_images
                      label: 📤📸 Select the uploaded images (Max. 9).
                      type: image
                      options:
                        multiple: true
                        max: 9